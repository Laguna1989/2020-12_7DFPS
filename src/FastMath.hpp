#ifndef FASTMATH_GUARD_HPP
#define FASTMATH_GUARD_HPP

#include <cstddef>

float mytan(float a);

float mytaninv(float a);

float mycos(float a);

float mysin(float a);

float wrapAngle(float a);

#endif
